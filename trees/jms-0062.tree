\title{Roladex}

\def\personal-queries[name]{
  \scope{
    \put\transclude/title{bachelor students (current and former)}
    \query{
      \query/and{
        \query/taxon{person}
        \query/meta{bachelor-advisor}{\name}
      }
    }
    
    \put\transclude/title{masters students (current and former)}
    \query{
      \query/and{
        \query/taxon{person}
        \query/meta{masters-advisor}{\name}
      }
    }
    
    \put\transclude/title{doctoral students (current and former)}
    \query{
      \query/and{
        \query/taxon{person}
        \query/meta{phd-advisor}{\name}
      }
    }
  
    \put\transclude/title{postdocs (current and former)}
    \query{
      \query/and{
        \query/taxon{person}
        \query/meta{postdoc-advisor}{\name}
      }
    }
  }
}

\p{A list of all people with pages in my forest.}

\query{
  \query/taxon{person}
}
